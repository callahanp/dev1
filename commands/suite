#!/bin/bash
# source only

# shellcheck enable=require-variable-braces


currentSuite=""
currentTask=""
# shellcheck disable=1091
source "${DEV1_COMMANDS_PATH}/context" "suite" "$@"

cd "${DEV1_SUITES_PATH}/${currentSuite}/tasks/${currentTask}" || return 1
